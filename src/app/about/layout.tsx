export default function AboutLayout({ children }: { children: any }) {
  return <main className='flex flex-col items-center justify-start bg-gradient-to-br from-gray-900 to-gray-950 overflow-y-auto'>{children}</main>
}
